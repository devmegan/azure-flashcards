export default function Submit({ question }) {
    return (
        <div className="card-actions mx-auto">
            <button className="btn btn-primary btn-outline">Check</button>
        </div>
    );
}